var GDM_GRID_VIEWER = (function(module){
    'use strict';
    var self = module.Submodule = module.Submodule || (function() {
      // [ private properties ]
      // [ private methods ]
      // [ public methods ]
    })();
    return module;
}(GDM_GRID_VIEWER || {}));